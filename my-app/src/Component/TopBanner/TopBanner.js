import { Box, Container, CssBaseline, Grid, Item } from '@mui/material';
import React from 'react';

const TopBanner = () => {
    return (
        <Grid container spacing={2}>
        <Grid item xs={8}>
            <h1> Hello </h1>
        </Grid>
        </Grid>
    );
};

export default TopBanner;